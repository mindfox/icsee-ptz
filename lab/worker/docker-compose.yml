services:
  icsee-ptz-web:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "${WEB_UI_BIND:-0.0.0.0}:${WEB_UI_PORT:-8095}:${WEB_UI_PORT:-8095}"
    volumes:
      - ../webui:/app:ro
      - ../../custom_components/icsee_ptz:/opt/icsee_ptz:ro
    restart: unless-stopped
